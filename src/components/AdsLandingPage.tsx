import LandingPage from './LandingPage'

/** Paid ad destination (`/landing`) — same conversion layout as `/`, with ad meta + UTM passthrough. */
export default function AdsLandingPage() {
  return <LandingPage variant="ads" />
}
